Documentation for the GLSL shader code included with the `r2` package.

Names starting with `R2_` are reserved by the `r2` package. All types
and functions are prefixed with this string to avoid conflicts with
user code.

